import {
    MuscleCategory,
    Equipment,
    ExerciseType,
} from "@/types/exercise_types";

export const MUSCLE_CATEGORIES: string[] = Object.values(MuscleCategory);
export const EQUIPMENT: string[] = Object.values(Equipment);
export const EXERCISE_TYPES: string[] = Object.values(ExerciseType);
